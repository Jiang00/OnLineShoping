<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.seocoo.onlineshoping">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activity.MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/DefaultCityPickerTheme"
            android:windowSoftInputMode="adjustUnspecified|stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.StoreDetailsActivity"
            android:windowSoftInputMode="adjustUnspecified|stateHidden" />
        <activity android:name=".activity.ScoreActivity" />
        <activity
            android:name=".activity.ConfirmOrderActivity"
            android:theme="@style/DefaultCityPickerTheme" />
        <activity android:name=".activity.ChooseReceiveAddressActivity" />
        <activity android:name=".activity.EditReceiveAddressActivity" />
        <activity android:name=".activity.ShopingCartActivity" />
        <activity android:name=".activity.CommodityDetailsActivity" />
        <activity
            android:name=".activity.SearchStoreActivity"
            android:windowSoftInputMode="stateVisible|adjustUnspecified" />
        <activity android:name=".activity.SuccessActivity" />
        <activity android:name=".activity.OrderListActivity"></activity>
    </application>

</manifest>